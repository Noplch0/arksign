name: Build & Release

on:
  push:
    branches: [latest]
    tags: ['v*']
  pull_request:
    branches: [latest]

permissions:
  contents: write
  actions: read

jobs:
  # =========================================================================
  # 代码检查 + 测试
  # =========================================================================
  lint-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true

      - name: Lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --timeout=3m

      - name: Test
        run: go test -v -race -coverprofile=coverage.txt ./...

  # =========================================================================
  # 全平台构建
  # =========================================================================
  build:
    name: Build (${{ matrix.os }}/${{ matrix.arch }})
    needs: lint-test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - os: linux
            arch: amd64
          - os: linux
            arch: arm64
          - os: darwin
            arch: amd64
          - os: darwin
            arch: arm64
          - os: windows
            arch: amd64

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true

      - name: Get version
        id: version
        run: echo "VERSION=$(git describe --tags --always --dirty 2>/dev/null || echo 'dev')" >> $GITHUB_OUTPUT

      - name: Build
        env:
          VERSION: ${{ steps.version.outputs.VERSION }}
        run: |
          BUILD_TIME=$(date -u '+%Y-%m-%dT%H:%M:%SZ')
          LDFLAGS="-s -w -X 'main.Version=${VERSION}' -X 'main.BuildTime=${BUILD_TIME}'"

          case "${{ matrix.os }}" in
            linux)   GOOS=linux   ;;
            darwin)  GOOS=darwin  ;;
            windows) GOOS=windows ;;
          esac

          EXT=""
          [ "${{ matrix.os }}" = "windows" ] && EXT=".exe"

          mkdir -p build
          CGO_ENABLED=0 GOOS=${GOOS} GOARCH=${{ matrix.arch }} \
            go build -ldflags "${LDFLAGS}" \
            -o "dist/arkSign_${{ matrix.os }}_${{ matrix.arch }}${EXT}" .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: arkSign_${{ matrix.os }}_${{ matrix.arch }}
          path: dist/*

  # =========================================================================
  # 发布（仅 tag 触发）
  # =========================================================================
  release:
    name: Create Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: arkSign_*
          path: artifacts
          merge-multiple: true

      - name: Generate release notes
        run: |
          echo "## 变更内容" > release_notes.md
          echo "" >> release_notes.md
          git log $(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo HEAD~1)..HEAD --oneline --no-merges >> release_notes.md 2>/dev/null || echo "* 初始发布" >> release_notes.md

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body_path: release_notes.md
          files: artifacts/**
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          token: ${{ secrets.GITHUB_TOKEN }}
